def transpose(matrice : list) -> list:
    return [list(col) for col in zip(*matrice)]

def calc_describe(grade : list) -> list:
    describe = {}
    t_grade = transpose(grade)
    for 
    return describe

test = [[1,2,3],[4,5,6]]
calc_describe(test)